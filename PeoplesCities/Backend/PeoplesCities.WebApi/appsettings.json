{
  // Строка подключения для контейнера Docker
  "DbConnection": "Host=postgres_db;Port=5432;Database=testpeoplescities;Username=postgres;Password=123",
  // Строка подключения для PostgreSQL
  //"DbConnection": "Host=localhost;Port=5432;Database=testpeoplescities;Username=postgres;Password=123",
  "WireMockSettings": {
    //"WireMockUrl": "http://localhost:8080"
    "WireMockUrl": "http://wiremock:8080"
  },
  //TODO: Реализоовать переключатель с PostgreSQL на SQLite через паттерн фабричный метод
  //"DbConnection": "Data Source=TestOIS.PeoplesCities.db",
  "AllowedHosts": "*",
  "IsSQLiteProDbProvifer": "false"
}
